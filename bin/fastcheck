#!/usr/bin/env ruby

require "bundler/setup"
require "tiny_dyno"

class SmallPerson
  include TinyDyno::Document

  hash_key :some_random, type: String

  field :first_name, type: String
  field :age, type: Integer

end

require 'pry'

@sm = SmallPerson.new(first_name: 'first', age: 20)

Pry.start
